<h1>Listing package test reactions</h1>

<table>
  	<tr>
		<td>Command</td>
		<td>Parameter</td>
		<td>Repetitions</td>
  	</tr>

	<% for ptr in @packages %>
  		<tr>
			<td><%= ptr.command %></td>
			<td><%= ptr.parameter %></td>
			<td><%= ptr.repetitions %></td>
    		<td><%= link_to 'Show', ptr %></td>
    		<td><%= link_to 'Edit', edit_package_test_reaction_path(ptr) %></td>
    		<td><%= link_to 'Destroy', ptr, :confirm => 'Are you sure?', :method => :delete %></td>
  		</tr>
	<% end %>
</table>

<br />

<%= link_to 'New package test reaction', new_package_test_reaction_path %>
